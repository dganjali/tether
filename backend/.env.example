# MongoDB Connection String
# For local MongoDB: mongodb://localhost:27017/shelter_system
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/shelter_system
MONGODB_URI=mongodb://localhost:27017/shelter_system

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
# Generate a strong secret key using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Server Port
PORT=3001

# Optional: Node Environment
NODE_ENV=development

# Optional: CORS Origin (for production)
# CORS_ORIGIN=http://localhost:3000
